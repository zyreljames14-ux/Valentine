<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Will You Be My Valentine? ❤️</title>

  <style>
    body {
      margin: 0;
      font-family: 'Segoe UI', sans-serif;
      background: linear-gradient(135deg, #ff758c, #ff7eb3);
      min-height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      color: white;
      text-align: center;
    }

    .card {
      background: rgba(255,255,255,0.15);
      padding: 30px;
      border-radius: 20px;
      width: 90%;
      max-width: 420px;
      backdrop-filter: blur(10px);
      box-shadow: 0 10px 25px rgba(0,0,0,0.25);
      position: relative;
    }

    h1, h2 {
      margin: 10px 0;
    }

    .photos {
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      gap: 10px;
      margin: 15px 0;
    }

    .photos img {
      width: 100%;
      height: 120px;
      object-fit: cover;
      border-radius: 15px;
    }

    .buttons {
      position: relative;
      height: 80px;
      margin-top: 20px;
    }

    button {
      padding: 12px 22px;
      border-radius: 30px;
      border: none;
      font-size: 1em;
      cursor: pointer;
      transition: transform 0.2s;
      position: absolute;
    }

    #yes {
      background: #4CAF50;
      color: white;
      left: 15%;
    }

    #no {
      background: #ff4d4d;
      color: white;
      right: 15%;
    }

    #yes:hover {
      transform: scale(1.1);
    }

    .hidden {
      display: none;
    }

    .heart {
      position: fixed;
      bottom: -20px;
      animation: float 6s linear infinite;
      pointer-events: none;
    }

    @keyframes float {
      0% { transform: translateY(0); opacity: 0; }
      10% { opacity: 1; }
      100% { transform: translateY(-100vh); opacity: 0; }
    }
  
  #finalMessage {
  margin-top: 15px;
  font-size: 1.05em;
  line-height: 1.6;
  animation: fadeIn 1.5s ease forwards;
}

@keyframes fadeIn {
  from {
    opacity: 0;
    transform: translateY(10px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}
  
  </style>
</head>

<body>

  <div class="card">
    <h1>Hi my love ❤️</h1>

    <div class="photos">
      <img src="photo1.jpg">
      <img src="photo2.jpg">
      <img src="photo3.jpg">
      <img src="photo4.jpeg">
    </div>

    <p>Every moment with you holds a very special
place in my heart, every laugh, every jokes that we made,
all the places weve been to, and all the smiles weve exchanged,
they were all special to me my love, I want to make
this day special too,      
     and I have one important question…</p>
    <h2>Will you be my Valentine? 💘</h2>

    <div class="buttons">
      <button id="yes">YES 💕</button>
      <button id="no">NO :(
      </button>
    </div>

    <h2 id="result" class="hidden">YAY!! I LOVE YOU SO MUCH 💖🥰</h2>
  </div>

  <p id="finalMessage" class="hidden">
  See you tomorrow at 10 AM in Ayala Central Block my lovee, 
  <br>Were gonna go on a date!!, paint!, have fun, and eatt!!<br>
    <br>Thank you for always being here for me baby, I love youu.<br>
  Happy Valentine’s Day, my sweet langga 💖
</p>
  
<script>
  const noBtn = document.getElementById("no");
  const yesBtn = document.getElementById("yes");
  const result = document.getElementById("result");
  const finalMessage = document.getElementById("finalMessage");

  yesBtn.addEventListener("click", () => {
    result.classList.remove("hidden");

    setTimeout(() => {
      finalMessage.classList.remove("hidden");
    }, 1200);

    createHearts();
  });

  function moveNoButton() {
    const x = Math.random() * 70;
    const y = Math.random() * 40;
    noBtn.style.left = x + "%";
    noBtn.style.top = y + "px";
  }

  ["mouseover", "mousedown", "touchstart", "click"].forEach(event => {
    noBtn.addEventListener(event, moveNoButton);
  });

  function createHearts() {
    setInterval(() => {
      const heart = document.createElement("div");
      heart.className = "heart";
      heart.innerHTML = "❤️";
      heart.style.left = Math.random() * 100 + "vw";
      heart.style.fontSize = Math.random() * 20 + 15 + "px";
      document.body.appendChild(heart);

      setTimeout(() => heart.remove(), 6000);
    }, 250);
  }
</script>

</body>
</html>
